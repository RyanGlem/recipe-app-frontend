import { Component } from "react";
import React from 'react'

const display = (props) => {

    return (

        <div> 
            <img src = {props.url} alt =""></img>
        </div>
        )
}

export default display